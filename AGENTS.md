# AGENTS.md

별도의 지침이 없다면 한국어로 응답합니다.

## 프로젝트 목적

이 프로젝트는 웹에서의 Color를 시각적으로 보여주기 위한 소개 웹사이트입니다. 색 공간, 색 좌표, 색상 모델, 대비, 보간, 웹 CSS 색상 표기처럼 추상적으로 느껴질 수 있는 개념을 텍스트 설명만이 아니라 실제 색, 움직임, 비교, 인터랙션으로 이해하게 만드는 것이 핵심입니다.

작업할 때는 "색을 설명하는 페이지"보다 "색을 직접 보면서 이해하는 화면"을 우선합니다. 새 섹션이나 기능은 가능한 한 스와치, 그라디언트, 캔버스, 3D 모델, 슬라이더, 토글, 샘플 코드, 실시간 결과를 함께 제공해야 합니다.

## 기술 스택

- Vite + React + TypeScript 기반입니다.
- 라우팅은 TanStack Router를 사용합니다.
- 스타일은 Tailwind CSS v4와 shadcn/ui 컴포넌트 기반입니다.
- 색상 계산과 변환은 `culori`를 우선 사용합니다.
- 3D 색 공간 시각화는 Three.js를 사용합니다.
- 아이콘은 `lucide-react`를 우선 사용합니다.

## 주요 명령어

- 개발 서버: `pnpm dev`
- 빌드 확인: `pnpm build`
- 타입 확인: `pnpm typecheck`
- 린트: `pnpm lint`
- 포맷: `pnpm format`
- 빌드 결과 미리보기: `pnpm preview`

변경 범위가 작더라도 TypeScript나 라우팅, 색상 계산, Three.js 렌더링을 건드렸다면 최소 `pnpm typecheck` 또는 `pnpm build`로 확인합니다. 시각적 변경을 했다면 브라우저에서 데스크톱과 모바일 폭 모두 확인합니다.

## 코드 구조

- 앱 진입점은 `src/App.tsx`입니다.
- 라우트 파일은 `src/routes/` 아래에 둡니다.
- `src/routeTree.gen.ts`는 TanStack Router가 생성하는 파일이므로 직접 편집하지 않습니다.
- 색 공간 모델, 샘플, 3D 렌더링 코드는 `src/color-models/` 아래에 둡니다.
- 재사용 UI 컴포넌트는 `src/components/ui/`의 기존 shadcn/ui 스타일을 따릅니다.
- 공통 유틸리티는 `src/lib/` 아래에 둡니다.
- 새 데모나 실험 화면은 기존 `src/playground/` 패턴을 먼저 확인하고 맞춥니다.

## 구현 원칙

- 색상 변환, 색상 공간 계산, 보간은 직접 수식을 흩뿌리기보다 `culori`나 명확한 도메인 함수로 모읍니다.
- 사용자가 조작할 수 있는 색상 예시는 현재 값이 어떤 색상 표기법인지 함께 보여줍니다. 예: `#RRGGBB`, `rgb()`, `hsl()`, `oklch()`.
- 단순 장식용 색보다 학습에 도움이 되는 색을 씁니다. 색이 무엇을 설명하는지 화면에서 바로 추론 가능해야 합니다.
- 색만으로 상태를 구분하지 않습니다. 라벨, 아이콘, 패턴, 위치, 수치 중 하나 이상을 함께 제공합니다.
- 중요한 텍스트와 컨트롤은 배경색 대비를 확인합니다.
- `useMemo`, `useCallback`, 별도 컴포넌트 추출은 실제 렌더링 비용이나 가독성 개선이 있을 때만 사용합니다.
- Three.js 리소스는 생성과 해제를 짝지어 관리합니다. geometry, material, texture, animation frame, event listener를 방치하지 않습니다.
- Canvas나 3D 화면은 빈 화면으로 실패하기 쉬우므로 렌더링 크기, device pixel ratio, resize 처리, cleanup을 함께 확인합니다.

## UI/UX 기준

- 첫 화면에는 프로젝트의 주제인 Color가 즉시 보여야 합니다.
- 텍스트 설명만 긴 페이지를 만들지 말고, 각 개념마다 직접 보이는 예시를 둡니다.
- 버튼, 탭, 토글, 슬라이더 같은 컨트롤은 기존 shadcn/ui 컴포넌트를 우선 사용합니다.
- 아이콘이 필요한 버튼은 가능하면 `lucide-react` 아이콘을 사용하고, 의미가 모호한 아이콘에는 접근 가능한 이름을 제공합니다.
- 카드 안에 카드를 중첩하지 않습니다. 반복 항목, 모달, 도구 패널처럼 실제로 프레임이 필요한 경우에만 카드 스타일을 씁니다.
- 색을 많이 다루는 웹사이트지만 한 가지 색조만 반복하는 팔레트는 피합니다. 모델 비교, 대비, 색상환, 보간 경로처럼 의도가 있는 색 다양성을 사용합니다.
- 모바일 폭에서도 텍스트, 탭, 축 라벨, 샘플 코드가 겹치지 않게 확인합니다.

## 문서와 설명

- 색상 개념 설명은 초보자도 이해할 수 있게 짧고 구체적으로 씁니다.
- 용어를 처음 사용할 때는 화면 예시와 연결합니다. 예: "Hue는 색상환에서의 각도"처럼 시각 모델과 함께 설명합니다.
- README나 화면 문구를 바꿀 때는 이 프로젝트가 템플릿이 아니라 Color 소개 웹사이트라는 점이 드러나게 유지합니다.

## 검증 체크리스트

시각적 변경 후 가능하면 다음을 확인합니다.

- `pnpm typecheck` 또는 `pnpm build`가 통과하는지
- 데스크톱 화면에서 주요 시각화가 비어 있지 않은지
- 모바일 폭 390px~402px 근처에서 텍스트와 컨트롤이 겹치지 않는지
- 색상 대비가 읽기 어려운 조합으로 바뀌지 않았는지
- Three.js 또는 Canvas 변경 시 resize와 cleanup이 정상 동작하는지
